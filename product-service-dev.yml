spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/product_db
    username: product_user
    password: ${PRODUCT_DB_PASSWORD:1234}
    driver-class-name: org.postgresql.Driver

# EUREKA
eureka:
  instance:
    non-secure-port: 19083

cloud:
  aws:
    s3:
      bucket: ${PRODUCT_S3_BUCKET:easymeal-dev-assets}
