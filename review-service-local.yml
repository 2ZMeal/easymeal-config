spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/review_db
    username: review_user
    password: ${REVIEW_DB_PASSWORD:1234}
    driver-class-name: org.postgresql.Driver
  kafka:
    bootstrap-servers: localhost:9092
